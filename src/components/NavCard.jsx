import React from 'react'

export default function NavCard({icon, callback}) {

    /*
        <div onClick={callback}>
            <img/>
        </div>
    */

  return (
    <div>NavCard</div>
  )
}
